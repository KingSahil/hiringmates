# 2026-09-13

- Analyzed HiringMates codebase (full static read, no runtime).
- Architecture: 3 units — Next.js 16/React 19/Tailwind v4 frontend (`app/`), Python FastAPI RAG backend (`backend/api.py` + `backend/rag/`), and `tools/github-detector` MCP stdio server.
- Backend URL is server-only (`lib/backend.ts`, no NEXT_PUBLIC); provider token forwarded from server. Plagiarism route has isomorphic TS fallback (`lib/proctoring/antiCheatEngine.ts`).
- Key gaps found: (1) committed migrations only cover RAG schema (`extractions/sessions/candidate_profiles/served_questions`); frontend realtime features need `rooms/room_members/messages/submissions/profiles` which have NO migration -> will fail on fresh Supabase. (2) `app/multiplayer.tsx` is dead/orphaned code (never imported; duplicate of `CodeMatesContent`). (3) `next.config.mjs` sets `typescript.ignoreBuildErrors: true`. (4) onboarding 400 message claims Google required but `buildIdentity` only requires GitHub.
- Report written to `CODEBASE_ANALYSIS.md`.

## Environment runbook (verified working 2026-09-13)
- Backend: `cd backend && ./.venv/Scripts/python.exe -m uvicorn api:app --host 127.0.0.1 --port 8000`
  - `/health` returns `{"ok":true,"problems":[],"scenarios":["candidate-onboarding"]}` when config valid.
  - `python-dotenv` was MISSING from backend venv and is required by `rag/config.py` — installed via
    `backend/.venv/Scripts/python.exe -m pip install python-dotenv`.
  - Backend is configured for **OpenAI** (`LLM_PROVIDER=openai`), not Gemini; `google-genai` not installed (fine).
- Frontend: `pnpm.CMD dev` -> http://localhost:3000 (Next 16 + Turbopack). Env loaded from root `.env`.
- Both `.env` (root) and `backend/.env` set `BACKEND_BASE_URL=http://127.0.0.1:8000` — keep backend on 8000.
- Port 8000 conflicts: a stale `python.exe` (old uvicorn) may hold it. Find with
  `netstat -ano | grep ':8000'`, kill with `taskkill /F /PID <pid>`.
- **pnpm/node_modules gotcha:** `gsap` and `@mediapipe/tasks-vision` were empty/missing dirs while pnpm's
  `node_modules/.modules.yaml` claimed all installed -> `pnpm install` said "Already up to date" and
  `pnpm install --force` failed. Fix: the real packages live in `node_modules/.pnpm/`; recreate links with
  `New-Item -ItemType Junction -Path node_modules\gsap -Target node_modules\.pnpm\gsap@3.15.0\node_modules\gsap`
  (same for `@mediapipe\tasks-vision`). Repo also has BOTH package-lock.json and pnpm-lock.yaml (mixed npm/pnpm).
- **Disk:** C: is at 100% (~4.2 GB free). `pnpm install --force` fails importing large optional platform
  binaries (`@img/sharp-libvips-linux-ppc64`) when disk is exhausted. Free space before reinstalling.
- Verified end-to-end: frontend `/api/plagiarism/check` returns `"source":"backend-llm"` (proves Next->Python
  bridge works, not the TS fallback). `/api/onboarding` correctly returns 401 without a Supabase session.
- Known gaps: frontend `.env` lacks `SUPABASE_SERVICE_ROLE_KEY` (so `/api/demo-account` will fail);
  `tools/github-detector/.env` not created (detector runs anonymously; set GITHUB_TOKEN for real use).

## Env setup completed (later same session)
- Added `SUPABASE_SERVICE_ROLE_KEY` to root `.env` (copied from `backend/.env`; same Supabase project).
  Next.js loads `.env` at process start -> **dev server must be restarted** after editing env for it to take effect.
  Verified: `POST /api/demo-account` now returns `{"ready":true}` (was broken before).
- Created `tools/github-detector/.env` from `.env.example` (GITHUB_TOKEN blank -> anonymous, 60 req/hr).
- Installed `pytest` into backend venv (was missing; `pyproject.toml` dev extra).
- Detector validated with the BACKEND venv interpreter (`backend/.venv/Scripts/python.exe`) — it has `requests`
  + `python-dotenv`, so `DETECTOR_PYTHON` can stay blank and the backend can shell out to `detect_cli.py` fine.

## Fixed: environment-dependent backend test
- `backend/tests/test_llm.py::TestProviderSelection::test_missing_sdk_or_key_raises_llmerror[openai]`
  failed once a real `.env` existed: `Settings(llm_provider=...)` picks up `OPENAI_API_KEY` from
  `backend/.env` (loaded into os.environ by python-dotenv), and the `openai` SDK is installed, so
  `build_llm()` succeeded instead of raising. Gemini/anthropic passed only because their SDKs are absent.
- Fix: blank the active provider's key explicitly (same convention `TestConfigValidation` already uses
  and documents), via a per-provider `blanked` dict. Now **49/49 tests pass**.

## Feature: auto-onboarding on first GitHub sign-in
Flow: sign in with GitHub -> if the account has never been registered -> start backend LLM pipeline
(rough profile + questions) -> land on /assessment -> standby for answers -> final enhanced profile.
- NEW `app/api/onboarding/auto/route.ts` (POST, idempotent):
  - "New" == no `candidate_profiles` row AND no `sessions` row for user_id.
  - Fail-CLOSED: if that lookup errors, return 500 rather than guessing "new" (a wrong guess would
    spawn duplicate extractions + LLM runs).
  - Requires a GitHub identity (`buildIdentity` missing list) -> else 400 `not_eligible`.
  - Creating the session is what starts the pipeline (backend returns 202, works in background).
- `components/AppShell.tsx`: fires once per mount after auth resolves; if response is `new` AND the
  user is on `home`, `setTab('assessment')`. Doesn't yank users off other tabs.
- `components/views/AssessmentContent.tsx`: adopts an in-flight session (resumable == new/pending/awaiting)
  and polls it, so refresh/reopen resumes. Completed sessions are skipped so the saved-profile view wins.
- Intake form stays inline + non-blocking during `pending` (existing render at ~line 344).

## Backend bug fixed: LLM returns `evidence` as a dict
- Symptom: sessions went `pending -> failed` with "RoughProfile ... evidence Input should be a valid
  list, input_value={'total_estimated_loc': ...}". The local OpenAI-compatible proxy
  (`OPENAI_BASE_URL=http://127.0.0.1:20128/api/v1`, model `antigravity/gemini-3.7-flash-high`, run via
  **Docker**) returns `evidence` as a stats dict, not a list.
- Fix: `_as_str_list()` helper + `field_validator("evidence", mode="before")` on `RoughProfile` and
  `SkillTag` in `backend/rag/models.py`. Matches the codebase's own stated philosophy (rubric scores
  kept as floats: "rejecting those fails the whole grade over a type that carries no meaning").
- Added `backend/tests/test_models.py` (6 tests). **55/55 pass.**

## IMPORTANT: LLM is a local Docker proxy — sessions hang if Docker is down
- `OPENAI_BASE_URL=http://127.0.0.1:20128/api/v1`. If the container is stopped, every LLM call times
  out after 60s and `api.py::_advance_safely` swallows the exception, so the session sits at `pending`
  FOREVER with no error surfaced. Symptom "session stuck on pending" == check Docker first.
- Verified full happy path once Docker was up: POST /sessions -> pending -> awaiting (rough profile +
  4 generated questions tailored to the profile) -> POST /answers -> **complete** with grade, verdict,
  tags (['CSS','HTML','frontend']) and skills. Test rows (auto-probe-*) were deleted afterwards.

## CodeMates: private lobbies (feature in progress)
- Decisions: private == **unlisted + join by exact code**; storage **Postgres + Realtime**;
  scope **core multiplayer** (create/join/leave, shared editor, cursors, chat, roster); **sign-in required**.
- The tables ALREADY EXIST in Supabase (created outside repo migrations), with exactly the columns the
  UI expects: `profiles(id,display_name,role,created_at)`, `rooms(id,code,host_id,title,status,created_at)`,
  `room_members(room_id,user_id,joined_at)`, `messages(id,room_id,user_id,body,created_at)`.
- **RLS on `rooms` was wide open** — verified: the demo user could read all 3 rooms incl. other users'
  codes (ROOM-FIFG, CM-UG7A). So lobbies were never private. Hardening requires DDL.
- **I cannot run DDL here**: no psql, no supabase CLI, no DATABASE_URL/DB password in .env. User must
  paste SQL into the Supabase SQL editor. (This is a standing environment limitation.)
- Wrote `backend/migrations/003_codemates_private_rooms.sql` (idempotent):
  drops ALL pre-existing policies on rooms/room_members/messages/profiles via a dynamic DO loop
  (named drops would leave old permissive policies behind), then adds host-or-member scoped policies,
  unique index on rooms.code, `is_room_member()` helper, and security-definer RPCs
  `create_room` / `join_room_by_code` / `leave_room`, plus a `handle_new_user` trigger that auto-creates
  a profile row on signup (with backfill) so display names and the messages->profiles join resolve.
- **Join-by-code MUST go through the RPC**: once RLS scopes rooms to host/member, a client SELECT by
  code cannot see a room it is not a member of.
- Frontend changes in `components/views/CodeMatesContent.tsx`: removed the 3 hardcoded demo rooms and
  the phantom "Custom Live Room" fallback; `loadRooms` now scopes to host-or-member via PostgREST
  `.or()`; create uses `create_room` RPC with duplicate-code retry (23505); join uses `join_room_by_code`
  with a real "No lobby found" error; both require sign-in; added `handleLeaveRoom` -> `leave_room` RPC
  (distinct from "Back to Rooms List", which keeps membership).
- `tsconfig.json` now excludes `backend/data` — the github-detector clone cache was being type-checked
  and produced hundreds of errors from unrelated cloned repos (react-router-dom, framer-motion, ...).
  tsc is clean (exit 0) after this. NOTE: `next build` hides TS errors anyway (ignoreBuildErrors: true),
  so always verify with `node node_modules/typescript/bin/tsc --noEmit --incremental false`.
- STATUS: frontend done + typechecks, but **NOT functional until the SQL is applied** (RPCs don't
  exist yet, so create/join will error).

## Company + mentor portal (IN PROGRESS)
Decisions: seeded password + allowlist; "passed round 1" = score on the card's questions vs a
threshold; autogenerated questions come from the **role + tags**; manual questions are **shuffled
per attempt**.
- DONE — backend:
  - `rag/scenarios.py`: new `POSITION_SCREENING` scenario ("position-screening"), registered in
    SCENARIOS. Prompts build from ROLE + POSITION DESCRIPTION + REQUIRED TAGS. mcq=4, theory=1.
  - `rag/pipeline.py`: `Pipeline.generate_position_questions(role, description, tags, mcq, theory)`
    — no session, no GitHub extraction, no rough profile; the role brief is the only input, which is
    what allows a fresh randomised set per attempt.
  - `api.py`: `POST /positions/questions`. **Strips `correct_index`** before returning so answers
    never reach the browser; grading must stay server-side.
  - Verified live: generated React/TypeScript questions from a Frontend Engineer brief.
  - 55/55 backend tests pass; /health now lists both scenarios.
- DONE — `lib/portal.ts`: hardcoded allowlist (COMPANY_EMAIL, MENTOR_EMAILS x4), `portalRoleFor()`,
  `isCompany`, `isMentor`, `companyForMentor`. **PLACEHOLDER ADDRESSES — must be replaced.**
- DONE — `backend/migrations/004_company_mentor_portal.sql` (needs pasting in Supabase):
  `portal_members` (seeded 1 company + 4 mentors; single source of truth for RLS — keep in sync with
  lib/portal.ts), `positions`, `position_questions`, `position_attempts`, `mentorship_slots`, plus
  `current_email()` / `portal_role()` helpers and RLS. Students can browse positions but can NEVER
  read `position_questions` (holds correct answers).
- STILL TO BUILD: company portal UI (create position: role/description/tags/threshold/auto-vs-manual
  + manual question entry), mentor portal UI (list passed students, book time slot -> notify),
  student attempt flow (answer questions, pass/fail vs threshold), and grounding the mentorship
  notification + private meeting join on the real `mentorship_slots.meeting_id`.

### Portal — BUILT (later same session)
- `lib/portal-server.ts`: `getSupabaseAdminClient()` (service role, typed loosely because the new
  tables have no generated types -> rows resolve to `never`), `studentActor()`, `portalActor(role?)`,
  `ownedPosition()`, `stripAnswers()`. Routes bypass RLS so they MUST call portalActor() first.
- API routes: `GET/POST /api/portal/positions`, `GET/POST /api/portal/positions/[id]/questions`,
  `POST /api/portal/attempts` (start; auto = fresh LLM set with include_answers=true, manual =
  shuffled pool; stores full set on the row, returns stripped), `POST /api/portal/attempts/[id]`
  (grade: MCQ-only scoring vs threshold), `GET/POST /api/portal/slots` (mentor lists passed students
  + books; student lists own slots).
- Backend: `/positions/questions` gained `include_answers` (internal only) so the Next server can
  grade. Browser responses always have correct_index stripped.
- UI: `PortalContent.tsx` (company: create position + manual question entry; mentor: passed students
  + book slot), `PositionsContent.tsx` (student: browse, sit round 1, see booked sessions).
  Routes `/portal`, `/positions` added; tabs wired into navigation.tsx + AppShell.
- Migration 004 was APPLIED by the user — positions/position_questions/position_attempts/
  mentorship_slots/portal_members all exist and portal_members is seeded with the 5 placeholder emails.
- REMAINING: (1) replace placeholder emails in lib/portal.ts AND the portal_members seed;
  (2) wire the mentor->student notification (bell) to the real slot so the student can join the
  private meeting via meeting_id; (3) seed the 5 Supabase auth accounts with a password.
- NOTE: codebase is being edited CONCURRENTLY — a `RecruiterPortal` view + `recruiter` tab appeared
  mid-work. Check current file state before editing navigation/AppShell.

### Portal — COMPLETED (notification + meeting join + seeded accounts)
- **Notification grounded**: removed the hardcoded "Sarah Vance / Maya Chen" demo notifications from
  `lib/notifications.tsx` (INITIAL_NOTIFICATIONS is now empty). The bell derives real notifications
  from `mentorship_slots` (GET /api/portal/slots) and subscribes to `postgres_changes` on
  `mentorship_slots` filtered by student_id, so a mentor's booking appears without a refresh.
  Slot read-state lives in its own localStorage key (`hiringmates_read_slots_v1`) because slots are
  server data. `markAsRead` handles the `slot-<id>` prefix.
- **Meeting join grounded**: `MentorshipMeetContent` no longer hardcodes
  'mentorship-r2-northstar' — it reads `?meeting=<id>` from the URL (fallback kept). The bell's
  handleJoinCall does setTab('mentorship') then `joinMeeting(meetingId)` which replaceState's the
  query. ORDER MATTERS: setTab rewrites the path, so the query must be stamped after.
- **Accounts seeded**: 5 Supabase auth users created (company@hiringmates.app, mentor1-4@…),
  email_confirm=true, password **HiringMates2026!**. All 5 verified SIGN IN OK. The
  `handle_new_user` trigger from migration 004 auto-created their profile rows.
- **Grading verified**: `/positions/questions` with `include_answers=true` returns
  `correct_index` ([1, 2] observed); without it the key is stripped (0 occurrences). The Next route
  stores the full set on `position_attempts.questions` and returns the stripped version.
- GOTCHA: pydantic ignores unknown request fields, so adding `include_answers` to api.py had NO
  effect until the backend was restarted — it silently stripped answers and grading would have
  scored 0. Always restart uvicorn after editing api.py.
- GOTCHA: launching uvicorn via `(cmd &)` inside a Bash tool call gets it killed when the call
  returns. Use the tool's run_in_background instead.
- REMAINING (needs the user): replace the 5 placeholder emails in `lib/portal.ts` AND the
  `portal_members` seed (already applied — needs an UPDATE), then re-seed the auth accounts.

### Portal — VERIFIED END TO END (10/10 checks)
Drove the real flow through the Next.js API routes with real Supabase sessions:
company creates position (201) -> student blocked from creating (403) -> student starts attempt
(201, 5 questions, answer key stripped) -> graded (score 100, passed) -> mentor sees the passed
student -> mentor books a slot (201) -> student sees the booked session with its meeting_id.
- **Two real bugs found by this test (both fixed):**
  1. `GET /api/portal/slots` gated on `portalActor()`, but students are intentionally NOT on the
     allowlist -> they were locked out of the very sessions booked for them. Now: mentor branch via
     `portalActor('mentor')`, otherwise fall through to `studentActor()` for own slots.
  2. `studentActor` was used in slots/route.ts without being imported -> runtime 500. Symptom was a
     500 with an EMPTY json body (Next returns an HTML error page, so `res.json()` yields {}).
     => A 500 with `{}` almost always means an unhandled throw, not a returned error.
- **E2E technique (reusable):** sign in via `POST {SUPABASE_URL}/auth/v1/token?grant_type=password`
  with the anon key, then call Next API routes with cookie
  `sb-<project-ref>-auth-token=base64-<base64(JSON.stringify(session))>`. Project ref = hostname
  part of SUPABASE_URL. This is what @supabase/ssr reads; verified against
  node_modules/@supabase/ssr/dist/main/cookies.js.
- Backend must be restarted after editing api.py (pydantic silently ignores unknown request fields,
  so new params like `include_answers` do nothing until restart).

### Changing the portal emails — one command
`tools/seed-portal-accounts.mjs` (new, dependency-free, uses fetch + Auth Admin REST):
- Reads the emails by PARSING `lib/portal.ts` (COMPANY_EMAIL / MENTOR_EMAILS), so the app and the
  seeded accounts can never drift apart. Edit lib/portal.ts, then run the script.
- Password via `--password` or `PORTAL_SEED_PASSWORD` (defaults to HiringMates2026! with a warning).
- Creates missing accounts, resets password + email_confirm for existing ones, never deletes.
- Prints (a) any stale @hiringmates.app auth users not in lib/portal.ts, with `delete from auth.users`
  SQL, and (b) ready-to-paste `insert into portal_members ... on conflict do update` SQL.
- Verified: correctly found and updated all 5 accounts.
So the 3-step change is: edit lib/portal.ts -> paste the portal_members SQL -> run the script.

### Landing behaviour (final)
- **Company + mentor**: on login they land straight on `/portal` (their dashboard). Added to
  `AppShell` as a once-per-mount redirect guarded by `portalRedirectRef`, so they can still navigate
  back to the home page afterwards without being bounced. Uses `portalRoleFor(user?.email)`.
- **Students**: home is unchanged. The "Hire Me" panel in `AuthorizedHome` now opens the position
  **marketplace** (`setTab('positions')`) instead of the old HireMe view. `HireMeContent` is still
  reachable from the CodeMates CTA, so the proctoring/plagiarism features are not orphaned.
- Marketplace copy updated in `PositionsContent`: "Find a badge. Apply.", CTA is **Apply**
  (was "Start round 1"), with a line explaining that applying starts round 1 and passing reaches a
  mentor.
- **Placeholder emails are KEPT by decision** — the user wants them to stay and the pipeline to keep
  working. Re-verified: company + mentor1 sign in (HTTP 200), portal_members still has all 5 rows.
  tools/seed-portal-accounts.mjs exists for the day they do want to swap them.
- tsc clean; /portal, /positions, / all 200.

### Marketplace had no cards (root cause: empty, not ordering)
- The user reported "marketplace should show the company cards first". Investigated: the cards
  section was ALREADY first in `PositionsContent` (order = header -> active attempt -> cards ->
  sessions). The real problem was that `positions` was EMPTY — the 10/10 E2E test deleted its test
  position during cleanup, leaving 0 rows, so students saw "No open positions yet."
- Fix: published a genuine card **through the company's own session** (not seeded fake data):
  "Frontend Engineer (React + TypeScript)", tags React/TypeScript/testing/REST, auto mode, pass 60%.
  Marketplace now returns 1 card.
- Made the empty state actionable ("No company has published a card yet. Sign in with the company
  account and open a position…") so an empty marketplace explains itself next time.
- LESSON: when a list view "doesn't show X", check whether the table actually has rows before
  reordering UI — the E2E cleanup step is what emptied it.

## Frontend design system (read app/globals.css before styling anything)
"Subtle, polished neo-brutalist". Tailwind v4 `@theme` tokens generate utilities directly:
- Colors: `--color-ink #171717`, `paper #fffaf0`, `lemon #ffd84d`, `aqua #39d5c8`,
  `berry #ff57ce`, `iris #6d73ff`, `grass #6ee56b`, `coral #ff6f61` -> `bg-lemon`, `text-ink`,
  `border-ink`, etc. all work.
- Fonts: `.font-display` = Bebas Neue (headings), body = Inter.
- Utilities: `.card-neo` (2px border, rounded-16, hard shadow, hover lift), `.btn-neo` + variants
  `btn-neo-lemon| aqua | berry | iris | paper | ink` (NO grass variant), `.shadow-hard`,
  `.shadow-hard-sm|lg|lemon|aqua|berry|iris`, `.border-3`, `.grid-paper` (dotted grid bg).
- Dark mode is handled by these classes under `html.dark` — use them instead of hand-rolled
  `dark:` variants.

## Redesigns (marketplace + portal + landing)
- `PositionsContent` rewritten as a neo-brutalist marketplace: `.grid-paper` bg, `.font-display`
  headings, `.card-neo` badge cards in a 2-col grid, per-card accent cycling lemon/aqua/berry/iris,
  tag chips, `.btn-neo-lemon` Apply CTA, numbered questions with lemon selected state.
- `PortalContent` rewritten: company (publish card + manual questions) and mentor (passed students +
  booking) in the same system. **Added a real sign-in screen** (email + password via
  `supabase.auth.signInWithPassword`) — company/mentor accounts are email+password and the landing
  button now points here, so without it they had no way in. Also handles "signed in but not on the
  allowlist" with a sign-out.
- Landing (`LandingContent`): both recruiter buttons (lines ~317 and ~713) repointed from
  `setTab('recruiter')` -> `setTab('portal')`, labels now "Company & Mentor Sign In".
- tsc clean; / /positions /portal all 200; marketplace still returns the published card.
