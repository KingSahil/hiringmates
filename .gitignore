# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
# NOTE: `.env*.local` does NOT match a plain `.env`, so `.env` is listed
# explicitly — otherwise a GitHub token would be committed and pushed.
.env
.env*.local

# Common ignores
node_modules
.next/
.DS_Store

# --- tools/github-detector (Python) ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.pytest_tmp/
# Clone cache for the repository analysis pass
tools/github-detector/data/
# TypeScript incremental build info (regenerated on every build)
tsconfig.tsbuildinfo
next-env.d.ts.bak
.env.bak

# Detector clone cache - clones embed their own .git repositories, so must never be committed
backend/data/
data/
cloudflared.exe
cloudflared.log
